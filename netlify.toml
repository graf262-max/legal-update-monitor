[build]
  functions = "netlify/functions"
  publish = "public"

[functions]
  node_bundler = "esbuild"

# 참고: 스케줄 기능은 나중에 별도 함수로 추가 예정

# 수동 테스트용 엔드포인트
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
